import './header.css'

export default function Header() {

    return (
        <div id={'header'}>
            <button>Portfolio</button>
            <button>Testground</button>
        </div>
    )
}